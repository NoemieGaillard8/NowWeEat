specVersion: "v1beta"
serviceId: "nowweeat"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "nowweeat-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
